
function showDetail(card) {
    card.style.transform = 'scale(1.1)';
    // Agrega cualquier otro efecto o contenido que desees mostrar al poner el ratón
}

function hideDetail(card) {
    card.style.transform = 'scale(1)';
    // Oculta o restablece cualquier contenido mostrado al quitar el ratón
}

